import './bootstrap';

import { Tooltip, Carousel, Collapse, Dropdown, Ripple, Tab, initTWE } from "tw-elements";
initTWE({ Tooltip, Carousel, Collapse, Dropdown, Ripple, Tab });



// import { Tooltip, Tab, Carousel, Ripple, Chart, Offcanvas, Dropdown, Collapse, LazyLoad, SmoothScroll, Stepper, initTE } from "tw-elements";
// initTE({ Tooltip, Tab, Carousel, Ripple, Chart, Offcanvas, Dropdown, Collapse, LazyLoad, SmoothScroll, Stepper });
